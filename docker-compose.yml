services:
  whatsapp-weather-agent:
    build:
      context: .
    image: whatsapp-weather-agent:local
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    volumes:
      - whatsapp-web-auth:/app/.wwebjs_auth
    restart: unless-stopped

volumes:
  whatsapp-web-auth:
